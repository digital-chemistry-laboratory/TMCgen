[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "tmcgen"
version = "0.0.0"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
include = ["tmcgen*"]
